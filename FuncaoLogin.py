import oracledb
def LoginAdm(senha,usuario):
    if usuario == "Admin":
        try:
            #input digite senha
            #verificar senha bate no banco
            #if senha = senha banco Interface.LoginAdm

